# Q2. Background Color – Fill the window with a blue background and keep it open until user clicks close.
import tkinter as tk

root = tk.Tk()
root.title("Group Project Game")
root.geometry("800x600")
root.configure(bg="blue")
root.mainloop()

# --- Build the GUI ---
root = tk.Tk()